{
  "name": "semantic-page-elements",
  "dockerComposeFile": "../compose.yml",
  "service": "dev",
  "workspaceFolder": "/workspace",
  "shutdownAction": "stopCompose",
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "denoland.vscode-deno"
      ],
      "settings": {
        "deno.enable": true,
        "deno.lint": true,
        "deno.unstable": false
      }
    }
  },
  "postCreateCommand": "sudo mkdir -p /home/vscode/.cache/deno /home/vscode/.cache/npm && sudo chown -R vscode:vscode /home/vscode/.cache && deno --version && deno task test",
  "remoteUser": "vscode"
}
