{
  "name": "semantic-page-elements",
  "dockerComposeFile": "../compose.yml",
  "service": "dev",
  "workspaceFolder": "/workspace",
  "shutdownAction": "stopCompose",
  "forwardPorts": [3000],
  "postStartCommand": "bash -lc 'set -euo pipefail; mkdir -p ~/.local/bin ~/.cache; ln -sf /usr/local/bin/claude ~/.local/bin/claude; bdui start --host 0.0.0.0 --port 3000 >> ~/.cache/bdui.log 2>&1 || true'",
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "denoland.vscode-deno"
      ],
      "settings": {
        "deno.enable": true,
        "deno.lint": true,
        "deno.unstable": false
      }
    }
  },
  "remoteUser": "vscode"
}
