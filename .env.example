# Copy this file to .env (which is gitignored) and fill in values.

# Required for git worktree support inside the container.
# Should point to the bare/common git dir that the worktree's .git file references.
BARE_REPO=/absolute/path/to/your/bare-or-common-git-dir.git

# Required for Beads (bd) persistence on the host.
BEADS_DIR=/absolute/path/to/your/beads/.beads

# Git identity inside the container (used for commits)
GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=you@example.com
